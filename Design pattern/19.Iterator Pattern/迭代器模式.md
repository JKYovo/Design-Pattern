### 迭代器模式

迭代器模式提供一种顺序访问聚合对象元素的方法，同时不暴露聚合对象内部的存储结构。

在示例中，草帽团内部用 `vector` 保存成员，但外部只通过迭代器的 `hasNext()` 和 `next()` 访问成员。以后容器从 `vector` 改成 `list`，客户端遍历方式也可以保持稳定。

适合使用迭代器模式的场景：

1. 需要遍历集合，但不想暴露集合内部结构。
2. 希望给同一个集合提供多种遍历方式。
3. 希望遍历逻辑从集合类中分离出来。
