### 解释器模式

解释器模式用于给一种简单语言定义语法表示，并提供解释执行的方法。每条语法规则通常对应一个表达式类。

在示例中，我们定义了一个很小的查询语言：`bounty Luffy` 查询赏金，`title Zoro` 查询称号。解析器根据命令创建不同表达式，表达式再结合上下文解释出结果。

适合使用解释器模式的场景：

1. 需要处理的语言或规则比较简单。
2. 语法规则可以清楚拆成类。
3. 规则变化频繁，但性能要求不高。
