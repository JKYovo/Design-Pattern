### 访问者模式

访问者模式把作用在一组对象上的新操作封装成访问者，让元素类保持稳定。元素类只需要提供 `accept()`，具体操作交给不同访问者完成。

在示例中，剑士、航海士、医生这些角色类不需要直接写战斗报告和悬赏报告逻辑。不同访问者进入角色对象后，完成不同类型的统计或展示。

适合使用访问者模式的场景：

1. 对象结构比较稳定，但经常需要增加新操作。
2. 操作和对象本身职责关系不大，不想塞进元素类。
3. 需要对一组不同类型对象执行统一流程。
