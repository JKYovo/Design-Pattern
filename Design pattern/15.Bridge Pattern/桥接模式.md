### 桥接模式

桥接模式用于把“抽象部分”和“实现部分”拆开，让它们可以独立变化。不要把所有组合都写成子类，而是让一个对象持有另一个抽象接口。

在示例中，船的类型是抽象部分，动力系统是实现部分。战斗船、侦察船可以搭配可乐引擎、风贝等不同动力。如果继续新增船型或动力系统，只需要扩展对应的一边，不需要爆炸式创建很多组合类。

适合使用桥接模式的场景：

1. 一个类存在两个或多个变化维度。
2. 使用继承会产生大量组合子类。
3. 希望抽象层代码不依赖具体实现。
