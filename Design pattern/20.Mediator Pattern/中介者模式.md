### 中介者模式

中介者模式用一个中介对象封装一组对象之间的交互，让对象之间不再直接相互引用，从而降低耦合。

在示例中，船员之间不直接保存彼此的指针，而是通过桑尼号这个中介者广播消息。新增船员时，只需要加入中介者，不需要让所有旧船员互相认识。

适合使用中介者模式的场景：

1. 多个对象之间存在复杂的网状通信。
2. 对象之间互相依赖导致难以维护。
3. 希望把交互规则集中放到一个地方管理。
