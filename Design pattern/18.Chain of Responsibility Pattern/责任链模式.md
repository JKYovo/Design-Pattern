### 责任链模式

责任链模式让多个处理者排成一条链，请求会沿着链传递，直到某个处理者能够处理它。发送者不需要知道最后是谁处理了请求。

在示例中，不同危险等级的事件先交给乌索普，再交给山治，最后交给路飞。每个角色只判断自己能不能处理，不能处理就交给下一个人。

适合使用责任链模式的场景：

1. 一个请求可能由多个对象处理。
2. 处理者和请求发送者不应该强耦合。
3. 处理顺序需要可以灵活调整。
